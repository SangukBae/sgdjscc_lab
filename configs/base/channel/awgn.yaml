# configs/channel/awgn.yaml – AWGN channel fragment
#
# Merged into composed configs via the _defaults_ list in load_config().
# All path values are resolved relative to the root config file's directory.

channel: awgn
snr_db: 10          # AWGN channel SNR (dB)

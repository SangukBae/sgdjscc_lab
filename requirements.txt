# sgdjscc_lab requirements – Phase 1 (AWGN inference only)
#
# This package runs inside the SGDJSCC original environment.
# All versions are pinned to match SGDJSCC/requirements.txt and install.sh.
# Install the full environment first:
#
#   conda create -n ptest python=3.9
#   conda activate ptest
#   conda install pytorch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 pytorch-cuda=11.8 -c pytorch -c nvidia
#
# Then run this requirements file:
#   pip install -r sgdjscc_lab/requirements.txt

# Core deep learning (must match SGDJSCC conda env)
torch==2.1.0
torchvision==0.16.0

# Diffusion + transformer infrastructure
diffusers==0.26.3
transformers==4.44.2
huggingface-hub==0.25.1
pytorch_lightning==2.4.0
timm==0.9.16
xformers==0.0.22.post7

# Utility / math
einops
omegaconf
numpy==1.23.2
scikit-image
Pillow

# Training console UX: rank-0 tqdm progress bar; NVML Python bindings for live
# GPU util/mem. Both degrade gracefully when absent (tqdm -> plain logs,
# NVML bindings -> util "na").
tqdm
nvidia-ml-py

# CLIP (OpenAI)
openai-clip

# Phase 1 uses these only for SGDJSCC imports; no evaluation loop here
lpips
pytorch-msssim
torchmetrics
clean-fid
torch-fidelity
efficientnet_pytorch
matplotlib

# ── Optional: Qwen2.5-VL caption generation (scripts/generate_captions.py --mode model) ──
# Bulk auto-captioning (e.g. SA-1B) uses Qwen/Qwen2.5-VL-3B-Instruct, a *native*
# transformers architecture that requires transformers>=4.49 — NEWER than the
# 4.44.2 pinned above for the paper-faithful BLIP-2 inference path. The two
# transformers versions cannot coexist, so install these into a SEPARATE
# caption-only environment (they are NOT needed for inference/eval/training):
#
#   pip install -U 'transformers>=4.49' accelerate qwen-vl-utils
#
# Only --mode model needs them; --mode fixed|filename run on the pinned env.
# transformers>=4.49
# accelerate
# qwen-vl-utils   # optional helper; the script does not import it
